name: Org Release

permissions:
  contents: write
  pull-requests: write
  issues: write

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  create-release:
    uses: Coalfire-CF/Actions/.github/workflows/org-release.yml@v0.1.1
